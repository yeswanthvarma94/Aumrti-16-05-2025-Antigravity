/**
 * OfflineBanner — kept for backwards compat but suppressed.
 * OfflineSyncProvider (in contexts/OfflineSyncContext.tsx) renders the
 * full-featured OfflineSyncBar at the bottom of the screen which shows
 * online status, queue count, and sync button.
 */
const OfflineBanner = () => null;

export default OfflineBanner;
